# Configuration

GardenPost uses the shared GardenCore database.

Keep the courier reward and route settings consistent with the server economy. Mailbox access is controlled by GardenLands rather than GardenPost.